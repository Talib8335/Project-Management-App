import { Calendar } from 'antd'
import AppLayout from '../AppLayout'

const Events = () => {
  return (
    <AppLayout>
        <div>
            <Calendar />
        </div>
    </AppLayout>
  )
}

export default Events
